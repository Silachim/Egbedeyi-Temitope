# About Page — Nigeria ⟷ United States Responsive Card Fix

Replace only:

`src/styles/global.css`

## What this fixes

- Desktop/wide screens: `Nigeria ⟷ United States` stays on one horizontal line.
- The card receives enough width to prevent `United States` from wrapping.
- Intermediate/tablet widths keep a centered horizontal card while the surrounding About section stacks.
- Mobile screens (520px and below): the layout intentionally becomes vertical, with the arrow rotated between the two country names.
- No About-page text, image, content, or other component is changed.
